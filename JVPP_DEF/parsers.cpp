#include "parsers.h"
#include "oclib.h"
#include "global.h"

/*
template<typename strType, typename fileNType>Core::Parser<strType, fileNType>::Parser()
{
	//assert(oclib::is_same_type(strType, std::string) || oclib::is_same_type(strType, std::wstring)&&"Incorrect strType ! (std::string or std::wstring only)");
}


template<typename strType, typename fileNType, typename charType> int Core::Parser<strType, fileNType, charType>::parseFile(fileNType file)
{
	OC_LOG_INFO << "Launching normal parsing on " << file;
	if (oclib::is_same_type(strType, std::wstring))
		return parseWFile(file);

	return 0;
}


template<typename strType, typename fileNType, typename charType> int Core::Parser<strType, fileNType, charType>::parseWFile(fileNType file)
{
	
}*/
